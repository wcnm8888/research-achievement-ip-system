# 下一阶段选项

更新时间：2026-07-26

## 整改路线入口

整改阶段四、阶段五和阶段六的文档收口已完成，UI 四项决策也已确认。当前下一项工作是按 Git 规范提交并远程验证工程基线，再对首个垂直切片任务卡进行批准和实现准备。

详细六阶段路线见 [`docs/project-management/roadmap.md`](../project-management/roadmap.md)，当前任务入口见 [`docs/project-management/current-task.md`](../project-management/current-task.md)。

## 当前推荐路线

### 当前入口：确认 UI 基线并准备浏览器验收

本入口以 `E:\Vibe coding\vibe-methodology\07-git-delivery.md`、`E:\Vibe coding\vibe-methodology\03-task-card.md` 和已确认的 UI 设计规格为依据，先验证 GitHub Actions/PR 工程基线，再批准首个垂直切片任务卡。此前浏览器检查只作为 UI 现状摸底，不替代正式验收。

当前入口产物应覆盖：

1. `.github/workflows/quality-gates.yml` 的 CI 门禁；
2. `.github/pull_request_template.md` 的 PR 验收模板；
3. `docs/project-management/current-task.md` 的首个垂直切片任务卡；
4. 任务卡的权限、持久化、状态机、幂等和浏览器验收标准；
5. 远程 CI 验证、任务卡批准和功能分支建立条件。

当前已确认的视觉方向是“冷静的科研档案工作台”：克制的蓝青主色、稳定的网格布局、可读的数据密度、明确的状态反馈和严格的权限提示。字体加载方式和浏览器视觉表现仍需在实现与验收中验证。

总整改路线固定为六阶段：产品定义、架构与 UI 定向、工程基线、按用户价值切片迭代、发布验收、复盘归档。阶段一至阶段六整改文档工作和 UI 决策已完成；GitHub Actions/PR 仍需远程验证，浏览器验收和首个垂直切片实现尚未完成。

### 后续：基于确认设计基线的浏览器视觉验收

在 Step 2 经过人工确认后，再使用桌面、平板和移动端浏览器验证设计实现、真实状态和核心流程。验收依据是 Step 2 的设计规格，而不是单纯检查页面能否打开。

## Route B：本地产品增强

用于继续完善本地产品体验，例如正式路由、API 状态管理、表格交互、报表能力、无障碍和性能优化。Route B 不代表生产部署或真实外部系统接入。

## Route C：真实外部系统对接准备

用于定义并验证 HR/SSO、邮件/短信、财务、文献、专利、DOI、对象存储等适配器契约、凭证边界、失败处理和联调计划。必须逐项授权，不默认连接真实系统。

## Route D：生产就绪准备

用于生产环境架构、数据库迁移、部署、监控、备份、灾备、安全、性能、发布和回滚准备。必须另行确认环境和操作权限，不能由本地测试或文档存在推导为生产就绪。

## 路线关系与共同边界

Route B、Route C、Route D 是不同目标，不合并成一条默认执行路线。当前先提交并远程验证工程基线，再批准首个垂直切片任务卡；实现后进行浏览器验收，并根据证据决定是否进入 Route B 的产品增强、Route C 的外部对接准备或 Route D 的生产就绪准备。

所有路线都必须遵守：源码和测试优先、最小修改、可验证门禁、敏感信息不入文档或仓库、生产和真实外部系统操作单独授权。

## 此前浏览器摸底的定位

此前浏览器检查已建立初步现状记录，但因本地 API 缺少 `DATABASE_URL` 未能完成正常数据和核心流程验收。该记录作为 Step 3 的输入，不作为 Step 2 设计基线的通过证据。

在工程基线远程验证、浏览器验收和首个垂直切片完成前，不进入生产就绪路线，也不把当前错误状态截图或页面可打开结论解释为业务流程完成。
