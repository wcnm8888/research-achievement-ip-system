# 实施计划

更新时间：2026-07-25

## 当前阶段状态

- 阶段一：仓库整理审计，已完成并归档。
- 阶段二：质量门禁与 Git 提交规范整改，已完成，整理提交已拆分并推送到 `origin/main`。
- 阶段三 Step 0：读取规则、建立源码与文档基线，已完成。
- 阶段三 Step 1：当前架构、UI 与需求权威文档刷新，本次完成。
- 整改阶段四：固定项目文档边界与长期文档收口，本次完成；未删除、移动、提交或推送。

本次 Step 只更新允许范围内的长期文档，没有修改业务代码、测试、配置或数据库 Schema，没有执行数据库、迁移、导入、生产、VPS 或真实外部系统操作，也没有提交、推送或合并。

## 整改阶段四收口结果

- `docs/` 被确认是当前项目长期文档区域。
- `docs/operations/` 被确认是长期运行、安全、备份、恢复和生产就绪资产，不属于临时过程文件。
- 新增 `docs/project-management/roadmap.md` 作为项目级路线入口。
- 新增 `docs/project-management/current-task.md` 作为唯一当前任务入口；当前没有正式功能任务卡。
- `architecture.md` 已包含当前数据模型概览和安全边界；本阶段不再创建重复的 `data-model.md`、`security-model.md`，是否拆分留给架构专项评审。
- 阶段归档仍只用于历史追溯和交接，不作为当前架构、需求、路线或任务的权威来源。
- 项目根 README、docs 索引、项目管理 README 和 AGENTS 已补充入口与边界说明。

阶段四完成不代表可以立即进行正式功能开发。当前仍需按 Git 规范处理未提交文档基线，并补齐 GitHub Actions、PR 门禁和首个垂直切片任务卡。

## 总整改路线（2026-07-25 对齐版）

本路线以产品价值切片为主线，避免继续使用“上一轮对话生成下一轮 Prompt”的无限递归流程。每个功能只使用一张任务卡，任务卡固定包含用户目标、非目标、输入输出、验收标准、数据与权限边界、UI 状态、测试矩阵、文件范围和完成定义。Step 记录只保留结论、证据和未解决风险，不再把聊天历史当作项目数据库。

| 阶段 | 目标和长期产物 | 当前状态 | 进入下一阶段的门禁 |
| --- | --- | --- | --- |
| 1. 产品定义 | 用户、核心流程、范围、非目标、验收标准；主要依据 `docs/requirements/product-brief.md` 和需求完成度矩阵 | 基线已存在，后续以任务卡继续细化 | 人工确认做什么、不做什么，以及核心验收标准 |
| 2. 架构与 UI 定向 | 当前架构事实、关键 ADR、数据与权限边界、UI/UX 方向、Style/Design System、核心页面规格 | 架构基线评审已收口；UI 规格和方向已确认 | 架构评审完成，UI 设计方向确认 |
| 3. 工程基线 | 仓库治理、唯一 GitHub 交付规范、CI、测试分层、环境说明和首个垂直切片 | 仓库整理和 Git 规范已完成；CI 与首个垂直切片未完成 | CI 在干净环境全绿，且首个切片通过独立 QA |
| 4. 功能迭代 | 按用户价值切片逐个交付，每张任务卡对应一个分支和一个 PR | 尚未开始新切片 | 定向测试、独立 QA、全量门禁、用户验收和 PR 证据齐全 |
| 5. 发布验收 | UAT、安全、性能、备份恢复、部署手册和发布检查表 | 未开始 | 发布检查表全部通过，生产操作另行授权 |
| 6. 复盘归档 | 最终证据、关键决策、剩余风险、token/返工/缺陷数据和最小阶段档案 | 未开始 | 人工验收完成，归档内容可交接、可复现 |

### 对当前项目整改工作的映射

1. 阶段一仓库资产审计和阶段档案整理已完成。
2. 阶段二测试修复、质量门禁、Git 仓库整理和提交拆分已完成；GitHub Actions、分支保护和 PR 模板仍属于阶段三工程基线，不能误写成已完成。
3. 阶段三 Step 0 和 Step 1 已完成，建立了源码优先的架构、UI、需求和项目管理文档基线。
4. 之前的浏览器检查重新定位为 UI 现状摸底，不是设计基线，也不是完整视觉验收。
5. 整改阶段六已完成架构与 UI 基线的文档评审，用户已确认 UI 方向；随后进行工程基线、首个垂直切片和基于设计规格的浏览器视觉验收。

### Token 和上下文控制门禁

- 不再读取完整历史 Prompt、全部 `progress.md` 或全部 `evidence.md` 作为每轮上下文；先定位当前 Step、最新决策和相关代码，再精读必要章节。
- 单次任务超过 3 次失败尝试、2 次上下文压缩或需要反复读取超过 20 个文件时，必须暂停并重新切片。
- 一个 Step 不能同时承担产品决策、架构决策、实现、测试、视觉验收和发布；跨职责内容拆成任务卡或后续阶段。
- 效率指标使用“每个通过验收的用户价值切片消耗多少 token、耗时多少、返工多少、缺陷多少”，不使用总 token 单独判断效率。

## 阶段三执行顺序

### Step 1：当前权威文档刷新（已完成）

已根据当前源码、Web 页面入口、API 模块、Prisma Schema、测试和 Git 基线刷新架构、技术栈、UI、需求矩阵、进度、证据、决策和下一阶段入口。

### 阶段六：架构与 UI 基线最终评审收口

结合 `E:\Vibe coding\references\UI` 和 `E:\Vibe coding\vibe-methodology\05-ui-design-system.md`，本阶段完成参考提炼、UX 流程、Style/Design System 和五个核心页面规格的文档评审。此前浏览器检查只作为 UI 现状摸底，不替代本阶段。

当前已形成并确认设计基线，方向为“冷静的科研档案工作台”；字体加载方式和浏览器视觉表现仍需在实现与验收中验证，不进行无任务卡的大规模 UI 重构。

### Step 3：基于已确认设计基线的浏览器视觉验收

以 Step 2 的设计规格为验收依据，检查桌面、平板、移动端、核心流程和完整 UI 状态；发现问题先记录，是否修复另行确认。

### Step 4：路由、状态与前端架构决策

根据 Step 2 的实际问题，决定正式路由、深链接、浏览器返回、API 请求、缓存和错误重试策略；先形成决策，再实施代码变更。

### Step 5：系统级角色、权限和业务流程验收

以角色矩阵、部门隔离、涉密访问、附件访问和审批流程为主线，补充跨模块验收证据。

## 约束

后续每个 Step 仍需先明确范围、权威上下文、验证方式和不做事项。生产系统、真实外部系统、真实身份和生产数据操作必须单独授权，不能由本阶段文档刷新推导出执行许可。
