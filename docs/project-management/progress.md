# 项目进度

更新时间：2026-07-26

## 已完成

- 阶段一仓库整理审计已完成。
- 阶段二质量门禁、测试修复、Git 整理与提交拆分已完成。
- 阶段二整理提交已推送，当前 `main` 与 `origin/main` 同步。
- 阶段三 Step 0 已完成：读取项目规则、README、当前源码、模块目录、页面入口、Schema 和相关长期文档。
- 阶段三 Step 1 已完成：刷新架构、技术栈、UI 规格、需求矩阵、项目计划、进度、证据、决策和下一阶段入口。
- 整改阶段四已完成：固定项目文档边界、补齐路线图和当前任务入口、明确运行手册与阶段归档边界。

## Step 1 结果

当前文档已改为以源码、Schema、测试和最新 Git 基线为权威，移除了把旧阶段过程、历史目录和过期“未实现”结论当作当前状态的表述。文档明确区分本地/demo/synthetic、mock、浏览器验收、生产和真实外部系统边界。

本 Step 没有修改业务代码、测试、配置或 Schema，没有执行生产操作，也没有提交或推送。

## 最近质量基线

- API、Web、Shared 测试通过。
- typecheck 通过。
- build 通过，但 Web 仍有大 bundle 警告。
- lint 通过。
- `git diff --check` 在本 Step 完成后执行。

## 首个垂直切片：成果草稿提交（2026-07-26）

本地实现和验收已完成，当前进入 Git 交付阶段。

- 真实数据库闭环：通过。使用本地 Docker PostgreSQL 和 staging API，未使用 mock 冒充持久化。
- 业务结果：成果可创建、保存、刷新恢复，并提交为 `PENDING_DEPARTMENT_REVIEW`。
- 一致性结果：审批实例 1 条、审批任务 1 条、提交动作 1 条、`SUBMIT` 审计记录 1 条；重复提交被拒绝。
- 权限结果：跨部门读取和修改均被拒绝。
- 浏览器结果：`1440 × 900`、`1024 × 768`、`390 × 844` 均完成页面操作/尺寸检查，无页面级横向溢出；部分接口返回 403 为预期权限拒绝。
- 全量门禁：API 1031、Web 440、Shared 1 个测试通过；typecheck、lint、build 通过。
- 构建边界：Web 仍有约 1.5 MB 主 bundle 警告，未作为本任务扩大范围处理。
- Git 交付：已完成两次功能提交和一次 CI 修复提交，已推送 `feat/achievement-draft-submit` 并创建 PR #4。
- 远程 CI：PR #4 的 `Quality Gates / Test, typecheck, build and lint` 已通过；修复点为在 CI 测试前生成 Prisma Client。
- 当前状态：PR #4 保持 Draft、无合并冲突、检查通过，等待用户审阅后决定是否转为 Ready for review 和合并。

## 当前风险

- UI 尚未完成独立浏览器视觉验收。
- 架构文档与代码曾经存在漂移，需要保持源码优先和定期刷新。
- Web 入口由 `App.tsx` 的 `activeKey` 分支管理，正式路由策略尚未决策。
- API 状态管理、请求缓存和重试策略尚未统一。
- 生产、真实外部系统和真实身份验收仍未完成。
- Web 构建仍有大 bundle 警告。

## 路线纠偏记录

此前浏览器检查已重新定位为 UI 现状摸底，不作为正式设计基线。阶段六已完成架构与 UI 基线的文档评审，用户已确认 UI 方向，浏览器验收仍需工程环境准备。

## 总整改路线对齐结果

本项目后续按“产品定义 → 架构与 UI 定向 → 工程基线 → 用户价值切片迭代 → 发布验收 → 复盘归档”六阶段推进。当前已完成阶段一仓库治理、阶段二质量与 Git 整理，以及阶段三的架构/需求/项目文档基线；尚未完成的工程基线包括 GitHub Actions、分支保护、PR 模板和首个垂直切片。今后不再由上一轮聊天自由生成下一轮 Prompt，而是由已批准的路线、任务卡和验收标准驱动。

本次正式 UI 基线采用“冷静的科研档案工作台”作为方向，先定义 UX、Style/Design System 和五个核心界面规格，再进入实现和浏览器验收。该方向、字体候选、Token 起点和核心页面优先级已由用户确认。

## 此前浏览器摸底结果

此前已完成桌面、平板、移动端的应用外壳、导航、权限状态和错误状态检查，并覆盖管理员角色的 13 个导航入口。由于本地 API 因缺少 `DATABASE_URL` 无法启动，正常数据和核心业务动作无法完成验收；该结果属于 Step 3 的预验收输入。

正式 Step 2 的下一步产物是：

1. 参考提炼和视觉方向；
2. UX 流程和信息架构；
3. Style/Design System；
4. 五个核心界面规格；
5. 人工确认后的 Step 3 浏览器验收标准。

## 阶段三 Step 2 与阶段六 UI 基线结果

- 已完成总整改路线与当前项目状态的对齐记录。
- 已完成 UI 参考原则提炼：参考积累、UX 先于 UI、Style/Design System 先于细节、截图/精确参数较真。
- 已完成五个核心界面草案：工作台、成果列表、成果详情与编辑、审批任务、系统配置。
- 已完成状态、权限、安全展示、响应式和浏览器验收标准草案。
- 阶段三 Step 2 当时尚待用户确认的视觉方向、Token 起点、字体候选和核心页面优先级，已在阶段六之后由用户确认。
- 本次没有修改业务代码、测试、配置或 Schema，没有启动生产/真实外部系统，没有提交或推送。

## 整改阶段四结果

- 当前项目长期文档区域确认为 `docs/`，其中 `docs/operations/` 是长期运行与安全资产。
- 新增 `docs/project-management/roadmap.md`，记录六阶段路线、当前映射和 Route B/C/D 边界。
- 阶段四当时新增 `docs/project-management/current-task.md`，用于记录当时“无正式功能任务卡”的状态；该入口现已更新为首个垂直切片任务卡。
- 当前 `architecture.md` 已同时包含数据模型概览、认证授权和安全边界；为了避免双权威，本阶段没有复制为两个新文件。
- 外部阶段档案只用于追溯和交接，Prompt 历史、完整日志和原始浏览器痕迹不进入当前长期文档。
- 阶段四之后仍不能直接开始正式功能实现：文档基线、GitHub Actions、PR 模板和首个垂直切片任务卡已在后续工程基线分支提交，仍需通过 PR 触发远程 CI。

## 整改阶段五结果（2026-07-25）

- 已将 `E:\Vibe coding\vibe-methodology\README.md` 固定为唯一方法论索引入口。
- 已建立 `02-methodology-router.md`、`03-task-card.md`、`06-test-selection-matrix.md` 和 `08-context-and-token-budget.md` 四个互不重复的执行入口。
- 已保留 `07-git-delivery.md` 作为唯一 Git/GitHub 交付规范，保留 `05-ui-design-system.md` 作为唯一 UI 设计规范，保留 `12-阶段复盘与归档.md` 作为唯一复盘归档规范。
- 已把本项目的 `current-task.md`、`roadmap.md`、`progress.md` 和 `decisions.md` 与新的方法论入口对齐；后续不再依据上一轮聊天输出自由生成下一步 Prompt。
- 任务卡现在要求一个用户价值切片对应一个任务卡、分支和验收闭环；测试先由需求、风险和错误边界推导；上下文预算规定最小读取集和失控停止条件。
- 本次没有修改业务代码、测试、配置或数据库 Schema，没有执行数据库、生产或真实外部系统操作，也没有提交、推送、合并、删除或重置。

## 阶段五之后的状态

方法论整改和阶段六架构/UI 文档评审已完成。工程基线的 CI 配置、PR 模板和首个垂直切片任务卡已提交并推送；远程 PR 验证、分支保护、功能实现和浏览器视觉验收仍需按路线单独推进。

## 整改阶段六结果（2026-07-26）

- 已复核当前 Web、API、Shared、Prisma 的真实职责、模块边界、数据流和身份/权限边界；当前模块化单体架构可作为后续任务基线。
- 已确认数据模型和安全模型继续作为 `docs/architecture/architecture.md` 的章节维护，未创建第二套权威文件。
- 已明确 `App.tsx` 的 `activeKey` 页面入口、正式路由策略、API 状态管理、缓存和重试策略仍属于后续架构决策，本阶段未修改代码。
- 已将当前 UI 外壳、13 个导航入口、`960px`/`640px` 响应式断点、状态模式和五个核心页面规格与源码及现有证据对齐。
- 已将阶段三 Step 2 仍被写成“下一步”的内容更新为阶段六评审结果；阶段三 Step 3 改为基于确认设计基线的浏览器验收入口。
- UI 视觉方向、Token 起点、字体候选和五个核心页面优先级已由用户确认；实际字体加载和浏览器视觉表现仍未验收。
- 本阶段没有修改业务代码、测试、配置或 Schema，没有执行数据库、生产或真实外部系统操作，没有删除、提交、推送、合并或重置。

## 阶段六之后的状态

当前应先在 GitHub 创建工程基线 PR 并验证 GitHub Actions；验证通过后，再批准首个垂直切片任务卡和建立功能实现分支，尚不能直接开始正式功能实现。

## 工程基线与首个任务卡（2026-07-26）

- 用户已确认 UI 视觉方向、Token 起点、字体候选和五个核心页面优先级。
- 已新增 `.github/workflows/quality-gates.yml`，在 `main` push 和面向 `main` 的 Pull Request 上执行安装、测试、typecheck、build 和 lint。
- 已新增 `.github/pull_request_template.md`，固定目标、范围、非目标、验收、门禁、浏览器证据、数据安全、风险和回滚信息。
- 已在 `docs/project-management/current-task.md` 建立首个垂直切片任务卡：科研人员创建成果草稿并提交审批。
- 任务卡覆盖成果草稿持久化、刷新恢复、权限拒绝、提交审批、工作流/审计一致性、幂等和三种浏览器尺寸；当前尚未开始实现。
- 本次已将工程基线分三批提交并推送到 `agent/engineering-baseline`，尚未创建 PR、合并或配置远程分支保护；GitHub Actions 仍需在 PR 中验证。
