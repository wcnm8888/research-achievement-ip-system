# Step 142 - 甲方视角代码冻结前 Punch List

日期：2026-07-07

审查基线：`f2d77f4 docs: verify latest client-facing web build`

## 一句话结论

当前还没有达到冻结代码的进度。

原因不是需求主体缺失，而是**真实演示入口仍在服务旧前端包**，还没有完成最终浏览器
截图验收。

## 需求文档要求是否实现

按“本地演示 / 竞赛评审 / synthetic 数据交付版”口径，核心需求已经覆盖：

| 需求能力 | 当前判断 | 甲方表达 |
| --- | --- | --- |
| 成果登记 | 已覆盖 | 可登记论文、专利、软件著作权等成果 |
| 审批流转 | 已覆盖 | 可提交、审批、归档、作废 |
| 权限与部门隔离 | 已覆盖 | 有角色权限、部门范围和安全边界 |
| 附件基础能力 | 已覆盖 | 展示附件安全摘要，不展示内部存储细节 |
| 费用管理 | 已覆盖 | 有费用台账、审核和凭证摘要 |
| 检索中心 | 已覆盖 | 可按成果与安全摘要检索 |
| 统计看板 | 已覆盖 | 可展示本地演示统计 |
| 审计日志 | 已覆盖 | 展示脱敏审计摘要 |
| 导入预检与导入记录 | 已覆盖 | 支持 CSV 预检、导入历史和行级安全摘要 |
| 账号与部门管理 | 已覆盖 | 支持账号、部门、生命周期安全摘要 |
| 自定义报表 | 已覆盖 | 可展示安全报表模板和结果 |
| 成果转化台账 | 已覆盖 | 可展示本地成果转化记录 |
| 涉密授权只读管理 | 已覆盖 | 可展示涉密资源和授权安全摘要 |
| 外部接口配置 | 已覆盖但仅 mock/adapter | 可演示配置和调用记录，不是真实外部系统联调 |

不能宣称完成的内容：

- 生产上线。
- VPS 验收。
- 生产 DB 验收。
- 真实 HR/SSO。
- 真实邮件短信。
- 真实财务系统。
- 真实 DOI / 文献库 / 专利平台。
- 真实对象存储。
- 生产监控、灾备、备份恢复演练、真实压测。
- 完整移动端验收。
- 涉密授权创建、撤销、审批、批量授权。
- 导入 retry、rollback、raw CSV/raw JSON、导出下载。

## 前端还有哪些内容必须处理

### P0：运行包刷新

必须先让当前演示入口使用最新前端包。

当前证据：

- `18081` 正在服务旧 bundle：`/assets/index-DTUeQ4MR.js`。
- 最新源码构建出的 bundle：`index-BuMvpDXh.js`。
- 最新构建包中，截图里的核心旧文案已经清理为 0。

因此下一步不是盲目继续改代码，而是：

1. 重建或刷新当前演示入口的 Web bundle。
2. 确认浏览器加载的是最新 bundle。
3. 再逐页截图验收。

### P0：逐页截图验收

刷新运行包后，逐页检查：

- 登录页。
- 工作台。
- 成果管理。
- 成果导入预检。
- 审批管理。
- 费用管理。
- 费用凭证附件。
- 检索中心。
- 统计看板。
- 自定义报表。
- 审计日志。
- 系统配置。
- 涉密授权管理。
- 账号管理。
- 部门维护。

每页都要确认普通可见文案不再出现：

- `GET /...`
- `POST /...`
- `X-Demo-User-Id`
- `dryRun=true`
- `dry-run`
- `Step xx`
- `local/demo`
- `not production`
- `Phase 1 frontend`
- `production auth`
- `session`
- `Failed to fetch`
- `Network request failed`
- raw JSON
- debug
- export
- download
- batch mutation

### P0：如果刷新后仍出现，再改源码

只有刷新后真实浏览器仍看到这些内容，才继续改 `apps/web/src/**`。

按页面分类处理：

- 顶部栏：不能出现工程状态标签或 session/auth 技术词。
- 左侧导航：全部使用中文业务名。
- 导入页面：只能叫“导入预检”，不能展示 `dry-run`、接口路径或请求参数。
- 权限提示：使用业务边界说明，不能展示 header、API 路径或调试说明。
- 附件与凭证：展示“安全摘要、校验值、内部存储标识摘要”等业务表达，不展示对象 key、下载链接或内部路径。
- 错误提示：展示“本地服务不可用/请稍后重试”等安全中文提示，不展示浏览器原始错误。

## 可以保留但需要解释的内容

这些不是普通页面文案，可以保留，但演示时要解释清楚：

- CSV 列名：`email`、`departmentCode`、`roleCode`。
- 安全校验码：`UNKNOWN_DEPARTMENT`、`DUPLICATE_IN_FILE`。
- 业务标准缩写：DOI。
- 内部请求 header 名称只允许存在于源码或网络层，不应作为页面文案展示。

## 推荐的下一步执行方式

### Route A：授权刷新本地 Docker Web 容器

适合你继续使用 `http://127.0.0.1:18081` 演示。

需要单独授权：

```text
允许只重建/替换现有 docker-compose.production.yml 管理的 web 容器；
不读取 .env / .env.production 内容；
不删除 volume；
不执行 prune；
不执行 down -v；
不清理 orphan；
不访问 production/VPS；
完成后只做 18081 浏览器验收。
```

### Route B：启动非 Docker 本地预览入口

适合避免 Docker 操作。

做法：

1. 使用最新本地构建包。
2. 通过非 Docker 本地静态服务或 Vite preview 暴露一个临时端口。
3. 如果需要 `/api`，只代理到当前可用的 `14001` 本地 API。
4. 验收后停止本 Step 启动的本地进程。

### Route C：只做文档冻结，不做 UI 冻结

不推荐作为最终交付。

如果时间不够，只能写成：

> 源码和测试已准备好；真实演示入口仍需刷新后截图验收。

不能写成：

> 前端 UI 已冻结。

## 当前冻结判断

- 需求主体：本地演示口径基本完成。
- 前端源码：最新构建包已清理核心旧文案。
- 当前运行入口：未刷新，仍有旧文案。
- 代码冻结：不通过。
- 下一步：刷新运行包并做浏览器截图验收。

## 边界

- 本文档是甲方视角冻结前清单。
- 未读取 `.env` 或 `.env.production` 内容。
- 未访问 production/VPS/生产 DB。
- 未调用真实外部系统。
- 未操作 Docker。
- 未修改 `apps/**`、`prisma/**`、schema 或 migration。
